for(const [x,y] of [[r.x-.4,r.y-.2],[r.x+r.w-.4,r.y-.2]]){let a=P(x,y);poly(c,[[a.x-12,a.y],[a.x,a.y+7],[a.x+12,a.y],[a.x,a.y-7]],'#778795');poly(c,[[a.x-9,a.y],[a.x-9,a.y-67],[a.x+9,a.y-67],[a.x+9,a.y]],grad(c,a.x-9,0,a.x+9,0,[th[2],'#91a3b1',th[2]]),'#9ab4c055');oval(c,a.x,a.y-68,14,6,th[1],'#b2c5cb77');star(c,a.x,a.y-43,4,th[3]);}const p=P(r.cx,r.cy);c.save();c.globalAlpha=.16;c.strokeStyle=th[3];c.lineWidth=1.2;c.beginPath();c.ellipse(p.x,p.y,110,63,0,0,Math.PI*2);c.stroke();star(c,p.x,p.y,37,th[3]);c.restore();}
 this.bg={can,ox:off,oy:80};this.mapCache=m;
 }
 glow(c,x,y,r,col){let g=c.createRadialGradient(x,y,0,x,y,r);g.addColorStop(0,col);g.addColorStop(1,'#00000000');c.fillStyle=g;c.fillRect(x-r,y-r,r*2,r*2);}
 draw(t){const w=this.world,r=w.run;if(this.mode==='home'||!r){const members=new Set(r?.units.map(u=>u.id)||[]),all=w.home.agents;w.home.agents=all.filter(a=>!members.has(a.id));this.homeRenderer.draw(t);w.home.agents=all;return;}let m=r.map,c=this.ctx,th=THEMES[m.theme];if(this.mapCache!==m)this.build(m);if(this.lastRun!==r.seed+':'+r.floor){this.lastRun=r.seed+':'+r.floor;this.center();}if(this.follow){let live=r.units.filter(u=>w.get(u.id).hp>0);if(live.length){let near=r.enemies.filter(e=>e.hp>0&&live.some(u=>Math.hypot(e.x-u.x,e.y-u.y)<6));let pts=[...live,...near].map(u=>P(u.x,u.y));let xs=pts.map(p=>p.x),ys=pts.map(p=>p.y),minX=Math.min(...xs),maxX=Math.max(...xs),minY=Math.min(...ys),maxY=Math.max(...ys);let tx=(minX+maxX)/2,ty=(minY+maxY)/2-34,z=Math.max(.5,Math.min(this.preferredZ,(this.w-35)/(maxX-minX+130),(this.h-90)/(maxY-minY+170)));this.camera.x+=(tx-this.camera.x)*.12;this.camera.y+=(ty-this.camera.y)*.12;this.camera.z+=(z-this.camera.z)*.055;}}
 c.setTransform(this.dpr,0,0,this.dpr,0,0);c.fillStyle=grad(c,0,0,this.w,this.h,['#101b30','#080f1b','#162238']);c.fillRect(0,0,this.w,this.h);this.hit=[];for(let i=0;i<40;i++){let x=(i*83.71+Math.sin(t*.08+i)*14)%this.w,y=(i*51.7-t*4)%(this.h+50);c.globalAlpha=.12+.12*Math.sin(t+i);oval(c,x,y,1,1,th[3]);}c.globalAlpha=1;
 c.save();c.translate(this.w/2-this.camera.x*this.camera.z,this.h*.55-this.camera.y*this.camera.z);c.scale(this.camera.z,this.camera.z);c.drawImage(this.bg.can,-this.bg.ox,-this.bg.oy);
 // Entrance and stairs are in-world structures, never a menu-only floor counter.
 let st=P(m.start.x,m.start.y);oval(c,st.x,st.y,32,18,null,'#adcfe977');star(c,st.x,st.y,15,'#a6d6f180');let ex=P(m.stairs.x,m.stairs.y);for(let i=0;i<5;i++)poly(c,[[ex.x-22+i*3,ex.y-3-i*5],[ex.x+i*3,ex.y+10-i*5],[ex.x+24+i*3,ex.y-3-i*5],[ex.x+2+i*3,ex.y-16-i*5]],r.cleared?'#b7dadd':'#738491','#3b4b60');if(r.cleared){this.glow(c,ex.x,ex.y-20,70,'#9ce7fb60');this.label(c,'다음 층 계단',ex.x,ex.y-46,'#b8f2f7');}
 for(let d of m.doors){if(d.open)continue;let p=P(d.x,d.y);poly(c,[[p.x-25,p.y],[p.x-25,p.y-48],[p.x,p.y-64],[p.x+25,p.y-48],[p.x+25,p.y]],'#283750','#bba477');line(c,p.x,p.y,p.x,p.y-57,'#8b9eb6');star(c,p.x,p.y-30,6,'#e2c392');}
 for(let p of m.props){let q=P(p.x,p.y);if(p.type==='torch'){poly(c,[[q.x-4,q.y],[q.x-4,q.y-36],[q.x+4,q.y-36],[q.x+4,q.y]],'#61768d');this.glow(c,q.x,q.y-36,58,'#ffd59c33');oval(c,q.x,q.y-39,4+Math.sin(t*13),9,'#ffcc79');oval(c,q.x,q.y-40,2,5,'#fff1b1');}if(p.type==='chest'){poly(c,[[q.x-17,q.y-16],[q.x+8,q.y-21],[q.x+20,q.y-13],[q.x+20,q.y+1],[q.x-5,q.y+8],[q.x-17,q.y+1]],p.taken?'#594d45':'#9c7851','#dec38f');line(c,q.x+2,q.y-18,q.x+2,q.y+4,'#dec58d',3);if(!p.taken)star(c,q.x+3,q.y-27,4+Math.sin(t*3),'#ffdc86');}if(p.type==='trap'){this.tile(c,p.x,p.y,'#a2535335','#d6796666');for(let i=-1;i<=1;i++)poly(c,[[q.x+i*10-3,q.y+2],[q.x+i*10,q.y-(p.cd>0?12:5)],[q.x+i*10+3,q.y+2]],'#b5bcc6');}if(p.type==='shrine'){poly(c,[[q.x-12,q.y],[q.x,q.y+6],[q.x+12,q.y],[q.x+9,q.y-26],[q.x-9,q.y-26]],'#668b98','#aad1df');if(!p.taken){this.glow(c,q.x,q.y-34,40,'#9af3d12f');star(c,q.x,q.y-35,10,'#b4f6d9');}}}
 for(let l of r.loot)if(!l.taken){let p=P(l.x,l.y);oval(c,p.x,p.y,7,4,l.kind==='gold'?'#e6be6b':'#c79de9');this.glow(c,p.x,p.y,24,l.kind==='gold'?'#e8c87540':'#e6a5ff40');}
 for(let e of r.enemies)if(e.cast){const q=P(e.cast.x,e.cast.y),sz=e.cast.radius;oval(c,q.x,q.y,sz*43,sz*26,'#ff4f5c38','#ff807c');oval(c,q.x,q.y,sz*43*(1-e.cast.time/1.5),sz*26*(1-e.cast.time/1.5),'#ff5a6240');}
 const actors=[...r.enemies.map(e=>({e,x:e.x,y:e.y,enemy:true})),...r.units.map(u=>({e:u,x:u.x,y:u.y,enemy:false}))].sort((a,b)=>a.x+a.y-b.x-b.y);
 for(const ob of actors){let q=P(ob.x,ob.y),e=ob.e,s=this.point(ob.x,ob.y);if(s.x<-100||s.x>this.w+100||s.y<-110||s.y>this.h+140)continue;c.save();c.translate(q.x,q.y);if(ob.enemy){if(e.hp>0||e.dead<3)monster(c,e,t);if(e.hp>0){this.bar(c,-18,e.boss?-116:-79,36,e.hp/e.maxHp,'#f58487');if(e.state==='교전'||e.boss)this.label(c,e.name,0,e.boss?-125:-88,'#ffbab4');}}else{let a=w.get(e.id);if(e.id===this.selected)oval(c,0,0,22,12,null,'#f3d99e');hero(c,a,t,e);this.bar(c,-18,-89,36,a.hp/a.maxHp,'#8be0cb');this.label(c,a.name+' '+a.level,0,-99,a.hp?'#d8edf0':'#ffa7af');}c.restore();if(!ob.enemy||e.hp>0)this.hit.push({kind:ob.enemy?'enemy':'hero',id:e.id,x:s.x,y:s.y-35*this.camera.z,r:24*this.camera.z});}
 for(let p of w.shots){let q=P(p.x,p.y);this.glow(c,q.x,q.y-30,12,p.hostile?'#ffb58f60':'#c5d1ff70');if(p.kind==='arrow')line(c,q.x-10,q.y-26,q.x+10,q.y-32,'#ffe8ab',2);else oval(c,q.x,q.y-30,4,4,p.hostile?'#ff9c82':'#d2c8ff');}
 for(let f of w.fx){const q=P(f.x,f.y),a=1-f.t/f.d;c.save();c.globalAlpha=a;let co=f.color||(['hurt','blast','trap'].includes(f.type)?'#ffa098':f.type==='heal'?'#a6f4cd':'#eddb9f');if(['heal','shield','portal','level','spin','blast'].includes(f.type)){oval(c,q.x,q.y,18+f.t*60,(18+f.t*60)*.6,null,co);this.glow(c,q.x,q.y-25,55,co+'33');}if(['slash','thrust','spin'].includes(f.type)){c.strokeStyle=co;c.lineWidth=4*a;c.beginPath();c.ellipse(q.x+10,q.y-30,40,20,.3,.1,Math.PI*1.4);c.stroke();}if(f.type==='bolt'){let pts=[[q.x-8,q.y-250],[q.x+17,q.y-180],[q.x-12,q.y-168],[q.x+5,q.y-94],[q.x-6,q.y]];pts.forEach((p,i)=>{if(i)line(c,...pts[i-1],...p,'#e9eeff',4);});this.glow(c,q.x,q.y-40,85,'#d7cfff70');}if(f.type==='meteor'){line(c,q.x-80,q.y-150,q.x,q.y-10,'#ddb9fa',4);oval(c,q.x,q.y-10,24,15,'#d9c9fb66');}if(f.type==='hit')for(let i=0;i<4;i++)star(c,q.x+Math.sin(i*4)*f.t*55,q.y-20+Math.cos(i*4)*f.t*30,3,co);if(f.text){c.font=(f.type==='level'?'bold 16':'bold 13')+'px system-ui';c.textAlign='center';c.fillStyle=co;c.strokeStyle='#142033';c.lineWidth=3;c.strokeText(f.text,q.x,q.y-62-f.t*26);c.fillText(f.text,q.x,q.y-62-f.t*26);}c.restore();}
 c.restore();this.mini();
 }
 bar(c,x,y,width,f,col){c.fillStyle='#0c1524';c.fillRect(x-1,y-1,width+2,5);c.fillStyle=col;c.fillRect(x,y,width*Math.max(0,f),3);}
 label(c,s,x,y,col){c.font='600 10px system-ui';c.textAlign='center';c.strokeStyle='#101a2c';c.lineWidth=3;c.strokeText(s,x,y);c.fillStyle=col;c.fillText(s,x,y);}
 mini(){let can=document.getElementById('mini');if(!can||!this.world.run)return;let c=can.getContext('2d'),r=this.world.run;c.fillStyle='#08121ef2';c.fillRect(0,0,136,124);for(let y=0;y<30;y++)for(let x=0;x<34;x++)if(r.map.grid[y][x]){c.fillStyle='#647d8b';c.fillRect(x*3.6+6,y*3.6+7,3.5,3.5);}for(let e of r.enemies)if(e.hp>0){c.fillStyle=e.boss?'#ffcf82':'#f37883';c.fillRect(e.x*3.6+4,e.y*3.6+5,e.boss?5:3,e.boss?5:3);}for(let u of r.units){c.fillStyle=this.world.get(u.id).hp>0?'#8ef9d6':'#77968f';c.fillRect(u.x*3.6+4,u.y*3.6+5,4,4);}let p=r.map.stairs;c.fillStyle='#ffdd8e';c.fillRect(p.x*3.6+4,p.y*3.6+5,4,4);}
 pick(x,y){if(this.mode==='home'||!this.world.run)return this.homeRenderer.pick(x,y);return this.hit.find(h=>Math.hypot(h.x-x,h.y-y)<Math.max(23,h.r));}
}
window.AstraView={View,hero,monster,portrait,P};
})();


(()=>{'use strict';
const {World,DUNGEONS,THEMES,MONSTERS,JOBS,K}=AstraAdventure,{View,portrait}=AstraView;
const $=id=>document.getElementById(id),escape=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
let world=new World(9072026),view=new View($('game'),world),running=false,paused=false,speed=1,panel=null,selected=1,inventoryFilter='all',peopleFilter='hero',queued=null,toastTimer=0,last=0,accum=0,refreshAt=0,saveAt=0,resultSeen=null,portraitCache=new Map(),audioOn=false,audioCtx=null;
const saved=World.load();$('continue').hidden=!saved;
const portraitURL=a=>{let k=a.id+':'+a.role;if(!portraitCache.has(k))portraitCache.set(k,portrait(a));return portraitCache.get(k);};
function toast(text){$('toast').textContent=text;$('toast').hidden=false;clearTimeout(toastTimer);toastTimer=setTimeout(()=>$('toast').hidden=true,4200);}
function answer(r){if(r)toast(r.msg||r.message||'완료');updateHUD(true);return r;}
function sound(freq=420){if(!audioOn)return;try{audioCtx=audioCtx||new(window.AudioContext||window.webkitAudioContext)();audioCtx.resume();let o=audioCtx.createOscillator(),g=audioCtx.createGain();o.type='sine';o.frequency.setValueAtTime(freq,audioCtx.currentTime);o.frequency.exponentialRampToValueAtTime(freq*.55,audioCtx.currentTime+.12);g.gain.setValueAtTime(.045,audioCtx.currentTime);g.gain.exponentialRampToValueAtTime(.001,audioCtx.currentTime+.18);o.connect(g);g.connect(audioCtx.destination);o.start();o.stop(audioCtx.currentTime+.2);o.onended=()=>{o.disconnect();g.disconnect();};}catch{}}
function adopt(w){world=w;view.world=w;view.homeRenderer.setGame(w.home);view.mapCache=null;view.lastRun='';view.homeRenderer.drawAgent=(c,a,t)=>AstraView.hero(c,a,t);selected=world.party[0]||1;view.selected=selected;portraitCache.clear();resultSeen=w.result;}
function begin(fresh){if(fresh&&saved&&!confirm('기존 v3 기록을 새 세계로 교체할까요? 이전 v1 기록은 그대로 남습니다.'))return;adopt(fresh?new World(Date.now()):saved||new World(Date.now()));running=true;paused=false;$('intro').hidden=true;close();if(fresh){answer(world.launch('dungeon',0,1));view.mode='battle';view.center();}else view.mode=world.run?'battle':'home';world.save();updateHUD(true);}
$('start').onclick=()=>begin(true);$('continue').onclick=()=>begin(false);
function close(){panel=null;$('sheet').hidden=true;document.querySelectorAll('.dock button').forEach(b=>