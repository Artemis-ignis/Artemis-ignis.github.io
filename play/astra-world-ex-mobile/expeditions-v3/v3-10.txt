/p><div class="bar"><i style="width:${s.progress/s.goal*100}%"></i></div><button data-act="watch">사건이 벌어지는 원정 관측</button></div>`:`<p>원정 중인 세계에 시험을 내립니다. 선택한 유형에 따라 <b>실제 적 또는 보물 상자</b>가 나타나며, 영웅들의 행동으로 완수·실패가 결정됩니다.</p><label>시나리오 이름</label><input id="scenarioName" maxlength="32" placeholder="예: 붉은 달이 뜨는 밤"><label>세계에 내릴 시험</label><select id="scenarioKind"><option value="hunt">사냥의 시험 · 추가 마물 처치</option><option value="invasion">붉은 달의 침공 · 침입자 전멸</option><option value="treasure">별의 보물 · 상자 2개 회수</option></select><label>강도</label><select id="scenarioDifficulty"><option value="1">1 · 작은 시험 / 보상 금화 70</option><option value="2">2 · 거센 시험 / 보상 금화 140</option><option value="3">3 · 가혹한 시험 / 보상 금화 210</option></select><button class="goldbtn" data-act="scenarioStart" style="width:100%" ${!world.run?'disabled':''}>세계에 시나리오 선포 · 신앙 40</button><p>${world.run?'현재 원정: '+escape(world.runName()):'먼저 던전 또는 탑 원정을 시작하세요.'}<br>제한시간 150초 · 유형과 이름·강도를 편집하는 방식입니다.</p>`}<h3>성역에 내리는 은혜</h3><div class="two"><button data-act="rain">비를 내려 풍요 후원 · 신앙 25</button><button data-act="provision">식량 보급 · 신앙 20</button></div><h3>세계 운영 정책</h3><div class="two"><button data-act="policy" data-kind="safe">안전 우선 권고</button><button data-act="policy" data-kind="bold">도전 우선 권고</button></div><p>현재: ${world.policy==='safe'?'안전 우선':world.policy==='bold'?'도전 우선':'균형'} · 전투의 후퇴 기준과 다음 층 판단에 반영됩니다.</p><h3>지나간 시나리오</h3>${world.scenarioHistory.map(e=>`<div class="logitem">${escape(e.name)} · ${e.ok?'완수':'종료'}</div>`).join('')||'<p>당신이 만드는 시험이 세계의 기억으로 남습니다.</p>'}`, 'scenario');}
const goals=[['첫 전투','마물 5마리 처치',w=>w.stats.kills>=5,40],['깊은 회랑','월석 회랑 최심부 완수',w=>!!w.clears.moon,100],['새로운 진로','한 명 이상 전직',w=>w.stats.promotions>0,70],['하늘을 향해','시련의 탑 10층 돌파',w=>w.tower>=10,180],['원정의 달인','서로 다른 던전 3곳 완수',w=>Object.values(w.clears).filter(Boolean).length>=3,240]];
function logPanel(){show('연대기와 목표',`<div class="stats"><span>처치<b>${world.stats.kills}</b></span><span>보스<b>${world.stats.bosses}</b></span><span>전직<b>${world.stats.promotions}</b></span><span>탑 돌파<b>${world.tower}</b></span></div><h3>성좌의 이정표</h3>${goals.map(([title,desc,test,reward],i)=>`<div class="card" style="margin-bottom:8px"><b>${title}</b><p>${desc} · 금화 ${reward}</p><button data-act="claim" data-id="${i}" ${world.claims.includes(i)||!test(world)?'disabled':''}>${world.claims.includes(i)?'보상 수령 완료':test(world)?'보상 받기':'진행 중'}</button></div>`).join('')}<h3>세계의 실제 기록</h3>${world.home.logs.slice(0,70).map(l=>`<div class="logitem"><small>${world.home.formatTime(l.time)} · ${ {combat:'전투',growth:'성장',divine:'성좌 개입',life:'생활',story:'이야기',danger:'위험',relation:'관계',faith:'신앙'}[l.type]||'기록'}</small>${escape(l.text)}</div>`).join('')}`, 'log');}
function settings(){show('관측 설정과 저장',`<div class="note">원정과 운명 v3.0<br>마스터의 기존 모바일 v1 저장과 고도 원본은 수정하지 않습니다. 이 버전의 원정·장비·층수는 별도의 v3 기록에 함께 저장됩니다.</div><div class="two"><button data-act="save">지금 저장</button><button data-act="export">저장 파일 내보내기</button><button data-act="import">저장 파일 불러오기</button><button data-act="sound">효과음 ${audioOn?'끄기':'켜기'}</button><button data-act="fullscreen">전체 화면</button><button data-act="home">성역 관측</button></div><h3>플레이 방법</h3><p>상단 <b>던전·시련의 탑</b>에서 원정 지도를 엽니다. 영웅이 스스로 탐색하고 공격하며 기술을 사용합니다.<br><br>하단 영웅 초상화를 누르면 레벨·경험치·전직·장비를 확인합니다. 레벨 5에 1차 전직, 레벨 15에 2차 각성을 후원할 수 있습니다.<br><br>한 손가락으로 지도 이동, 두 손가락으로 확대·축소, 「따라가기」로 교전 중인 영웅을 다시 관측합니다.<br><br>성역을 관측해도 원정은 계속됩니다. 자율 진행을 켜두면 다음 층 탐색뿐 아니라 귀환 후 휴식·장비 준비·다음 원정도 영웅들이 이어갑니다. 화면을 성역에서 강제로 바꾸지는 않습니다.<br><br>브라우저를 떠나면 시간은 멈춥니다. 자동 저장은 현재 브라우저에 남습니다.</p><h3>현재 제작 범위</h3><p>이것은 원본 고도 프로젝트를 모바일로 완전 이식한 버전이 아닌 별도 웹 실행본입니다. 탑은 100개 층의 절차 생성 구조이며, 100층 모두 수작업으로 만든 지도는 아닙니다. 세계와 움직이는 인물은 벡터 기반 2차원 표현이며, 시작 영웅 초상화는 이 대화에서 생성한 그림을 재가공했습니다. 회화풍 애니메이션의 완성판은 아닙니다.</p>`, 'settings');}
function resultPanel(){let r=world.result;if(!r)return;show(r.success?'원정 완수':'성역으로 생환',`<div class="eyebrow">${escape(r.name)}</div><div class="stats"><span>처치<b>${r.kills}</b></span><span>금화<b>${r.gold}</b></span><span>경험치<b>${r.xp}</b></span><span>장비<b>${r.itemIds.length}</b></span></div><p>획득 경험치는 전투 도중 각 영웅에게 반영됐습니다. 장비는 보관함에, 금화는 귀환과 동시에 자원에 반영됩니다.</p><div class="two"><button class="goldbtn" data-act="bag">획득 장비 확인</button><button data-act="heroes">성장·전직 확인</button><button data-act="map">다음 원정</button><button data-act="home">성역에서 회복</button></div>`, 'result','하나의 원정 · 하나의 저장 상태');}
function building(id){let b=world.home.terrain.building(id);if(!b)return;show(b.name,`<p>${escape(b.desc)}</p><div class="note">시설 ${1+(world.home.upgrades[id]||0)}단계 · 주민들이 자신의 욕구에 따라 이용합니다.</div><button style="width:100%" data-act="buildingUp" data-id="${id}">시설 확장 후원</button><h3>지금 성역의 생활</h3>${world.roster.filter(a=>!world.run?.units.some(u=>u.id===a.id)).slice(0,12).map(a=>`<div class="logitem">${escape(a.name)} · ${escape(a.action)}</div>`).join('')}`, 'building');}
function open(kind){if(kind==='map')mapPanel();else if(kind==='heroes')heroesPanel();else if(kind==='bag')bagPanel();else if(kind==='scenario')scenarioPanel();else if(kind==='log')logPanel();else if(kind==='tower')towerPanel();else settings();}
function updateHUD(force=false){const r=world.run,res=world.home.resources;$('faith').textContent=Math.floor(res.faith);$('gold').textContent=Math.floor(res.gold)+(r?` +${r.gold}`:'');$('food').textContent=Math.floor(res.food);$('pause').textContent=paused?'▶':'Ⅱ';$('speed').textContent=speed+'배';$('auto').innerHTML='자율 진행<br><b>'+(world.auto?'켜짐':'꺼짐')+'</b>';
 const battle=view.mode==='battle'&&r;$('location').textContent=battle?world.runName():'별빛 성역';$('regionLabel').textContent=battle?(r.kind==='tower'?'시련의 탑 · 실제 층 탐색':'던전 원정 · 실시간 관측'):'생활 · 훈련 · 제작 · 관계';$('progressText').textContent=battle?`${r.retreat?'귀환 신탁 · 입구로 이동':r.phase} · 남은 적 ${r.enemies.filter(e=>e.hp>0).length} · 처치 ${r.kills}${queued?' · 다음 원정 예약':''}`:`체류 ${world.roster.length-(r?.units.length||0)}명 · 원정 ${r?.units.length||0}명${r?' · '+world.runName():''}`;
 $('miniWrap').hidden=!battle;$('watch').textContent=r?(battle?'세계 지도':'⚔ 전투 관측'):'새 원정';$('floorDone').hidden=!(battle&&r.cleared);$('floorDone').querySelector('b').textContent=r?.cleared?(world.auto?'층 돌파 · '+Math.max(0,Math.ceil(6-r.clearTimer))+'초 후 자율 판단':'층 돌파 · 다음 목적지를 기다립니다'):'층 돌파';
 let boss=battle?r.enemies.find(e=>e.boss&&e.hp>0&&r.units.some(u=>Math.hypot(u.x-e.x,u.y-e.y)<10)):null;$('boss').hidden=!boss;if(boss)$('boss').innerHTML=`${escape(boss.name)} · ${Math.ceil(boss.hp)}/${boss.maxHp}<i style="width:${boss.hp/boss.maxHp*100}%"></i>`;
 document.querySelectorAll('.maps button').forEach(b=>b.classList.toggle('active',b.dataset.nav===(battle?(r.kind==='tower'?'tower':'dungeon'):'home')));
 const party=world.run?world.run.units.map(u=>u.id):world.party;const key=party.join(',');if(force||$('partyStrip').dataset.key!==key){$('partyStrip').dataset.key=key;$('partyStrip').innerHTML=party.map(id=>{let a=world.get(id);return`<button class="unitCard ${a.id===selected?'active':''}" data-act="hero" data-id="${id}" aria-label="${escape(a.name)} 성장 정보"><img src="${portraitURL(a)}" alt=""><span class="level" id="lv${id}">${a.level}</span><span class="unitName">${escape(a.name)}</span><span class="hp"><i id="hp${id}"></i></span><span class="xp"><i id="xp${id}"></i></span></button>`;}).join('');}
 for(let id of party){let a=world.get(id);$('lv'+id).textContent=a.level;$('hp'+id).style.width=a.hp/a.maxHp*100+'%';$('xp'+id).style.width=a.xp/world.xpNeed(a)*100+'%';$('lv'+id).closest('button').classList.toggle('ready',a.level>=5&&!a.promotion&&a.hero);}
 document.querySelectorAll('[data-power]').forEach(b=>{let cd=world.powerCD[b.dataset.power]||0;b.disabled=cd>0;b.querySelector('small').textContent=cd>0?Math.ceil(cd)+'초':({heal:28,bless:22,bolt:35,oracle:15}[b.dataset.power]);});
 if(panel==='hero'){let a=world.get(selected);if(a&&$('heroHP')){$('heroLevel').textContent=a.level;$('heroHP').textContent=`체력 ${Math.ceil(a.hp)} / ${a.maxHp}`;$('heroXP').textContent=`경험치 ${Math.floor(a.xp)} / ${world.xpNeed(a)}`;$('heroHPbar').style.width=a.hp/a.maxHp*100+'%';$('heroXPbar').style.width=a.xp/world.xpNeed(a)*100+'%';document.querySelectorAll('[data-act="promote"]').forEach(b=>b.disabled=a.level<5);}}
 if(world.alerts.length){$('sceneNotice').textContent=world.alerts.pop();world.alerts=[];}else if(!$('sceneNotice').textContent)$('sceneNotice').textContent='관찰 중 · 영웅은 스스로 판단합니다';
 if(world.result&&world.result!==resultSeen){resultSeen=world.result;if(view.mode==='battle'&&!queued){resultPanel();view.mode='home';}else toast('원정대가 귀환했습니다. 기록에서 보상을 확인하세요.');world.save();}
}
$('closeSheet').onclick=close;$('settingsBtn').onclick=settings;$('pause').onclick=()=>{paused=!paused;updateHUD();};$('speed').onclick=()=>{speed=speed===1?2:speed===2?4:1;updateHUD();};$('auto').onclick=()=>{world.auto=!world.auto;world.autoExpedition=world.auto;updateHUD();};$('watch').onclick=()=>world.run&&view.mode==='home'?observe('battle'):mapPanel();$('follow').onclick=()=>{view.follow=true;view.center();if(view.mode==='home'){view.homeRenderer.follow=true;view.homeRenderer.selected=selected;}toast('관측 카메라가 선택 영웅을 따라갑니다.');};
function zoom(f){if(view.mode==='home')view.homeRenderer.zoomAt(f);else {view.camera.z=AstraEngine.clamp(view.camera.z*f,.25,1.8);view.preferredZ=view.camera.z;}}
$('zoomIn').onclick=()=>zoom(1.2);$('zoomOut').onclick=()=>zoom(1/1.2);
function rerender(){const scroll=$('sheetBody').scrollTop;if(panel==='hero')heroPanel(selected);else if(panel==='bag')bagPanel();else if(panel==='heroes')heroesPanel();else if(panel==='log')logPanel();else if(panel==='scenario')scenarioPanel();$('sheetBody').scrollTop=scroll;}
document.addEventListener('click',ev=>{let b=ev.target.closest('button');if(!b||b.disabled)return;if(b.dataset.sheet){open(b.dataset.sheet);return;}if(b.dataset.nav){let nav=b.dataset.nav;if(nav==='home')observe('home');else if(nav==='dungeon'&&world.run?.kind==='dungeon')observe('battle');else if(nav==='tower'&&world.run?.kind==='tower')observe('battle');else if(nav==='tower')towerPanel();else mapPanel();return;}if(b.dataset.powe